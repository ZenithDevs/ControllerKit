{
	"name": "controllerkit",
	"version": "1.0.0",
	"description": "",
	"main": "dist/controllerkit.js",
	"publishConfig": { "registry": "https://npm.pkg.github.com/" },
	"scripts": {
		"start": "rm -rf dist && mkdir dist && rollup -c -w",
		"build": "rm -rf dist && mkdir dist && rollup -c"
	},
	"keywords": ["typescript", "javascript", "gamepad", "controller"],
	"author": "Magnetar",
	"license": "ISC",
	"devDependencies": {
		"@rollup/plugin-typescript": "^4.1.1",
		"rollup": "^2.6.1",
		"tslib": "^1.11.1",
		"typescript": "^3.8.3"
	}
}