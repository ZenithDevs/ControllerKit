{
  "name": "@zenithdevs/controllerkit",
  "version": "1.0.4",
  "description": "A simple JavaScript library to serve as a common API for touch, keyboard, and gamepad inputs.",
  "main": "dist/controllerkit.js",
  "publishConfig": {
    "registry": "https://npm.pkg.github.com/"
  },
  "types": "./types/controllerkit.d.ts",
  "scripts": {
    "dev": "rm -rf dist && mkdir dist && rollup -c -w",
    "build": "rm -rf dist && mkdir dist && rollup -c"
  },
  "keywords": [
    "typescript",
    "javascript",
    "gamepad",
    "controller"
  ],
  "author": "Magnetar",
  "license": "ISC",
  "devDependencies": {
    "@rollup/plugin-typescript": "^8.2.1",
    "rollup": "^2.44.0",
    "tslib": "^2.1.0",
    "typescript": "^4.2.3"
  }
}
